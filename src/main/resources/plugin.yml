name: ${name}
description: ${description}
author: ${author}
main: ${mainClass}
version: ${version}
url: ${url}
folia-supported: true
commands:
  koth:
    description: Main koth command
    usage: /koth <start|stop|status|create|delete|edit|finish|list|tp|join|leave|set|reload>